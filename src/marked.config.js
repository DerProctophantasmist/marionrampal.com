// Generated by CoffeeScript 1.12.6
(function() {
  var Config;

  module.exports = 'markedConfig';

  Config = require('./config.data.js');

  require('angular').module('markedConfig', [require('angular-marked'), require('./oEmbed'), require('./config'), require('./resource.file')]).config([
    'markedProvider', 'OEmbedUrl', 'ResourceFile', function(markedProvider, OEmbedUrl, ResourceFile) {
      var marked, oldRenderer, renderer;
      marked = require('marked');
      oldRenderer = new marked.Renderer();
      renderer = {
        link: function(href, title, text) {
          var res;
          if ((res = OEmbedUrl(href)) || (res = ResourceFile(href, title, text))) {
            return res.html;
          } else {
            return oldRenderer.link(href, title, text);
          }
        },
        heading: function(text, level, raw) {
          var escapedText;
          if (level > 3) {
            return oldRenderer.heading(text, level, raw);
          }
          escapedText = text.toLowerCase().replace(/[^\w]+/g, '-');
          return '<h' + level + ' class="special-font" ><a name="' + escapedText + '" class="anchor" href="#' + escapedText + '"><span class="header-link"></span></a>' + text + '</h' + level + '>';
        }
      };
      markedProvider.setOptions({
        gfm: true
      });
      return markedProvider.setRenderer(renderer);
    }
  ]);

}).call(this);

//# sourceMappingURL=marked.config.js.map
